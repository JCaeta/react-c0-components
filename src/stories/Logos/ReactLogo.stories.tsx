import React from 'react';
import {ReactLogo} from '../../index';

export default
{
    title: 'Logos/ReactLogo',
    component: ReactLogo,
}

const Template = (args: any) => <ReactLogo {...args} />;

export const Primary = Template.bind({});
Primary.args = 
{

}
